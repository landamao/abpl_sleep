{
  "休息设置": {
    "description": "休息（睡觉）行为设置",
    "type": "object",
    "hint": "控制 AI 单次休息的时长上限与醒来后的提示语",
    "items": {
      "最长休息小时": {
        "type": "float",
        "description": "单次休息的时长上限（小时）",
        "default": 12,
        "hint": "LLM 请求的休息时长超过该值时，本次睡觉请求会被拒绝",
        "slider": {
          "min": 1,
          "max": 24,
          "step": 0.5
        }
      },
      "最短休息秒": {
        "type": "int",
        "description": "单次休息的时长下限（秒）",
        "default": 5,
        "hint": "请求的休息时长不足时自动补到该值，防止秒睡秒醒",
        "slider": {
          "min": 5,
          "max": 600,
          "step": 5
        }
      },
      "暂存消息上限": {
        "type": "int",
        "description": "睡觉期间单个会话最多暂存的消息条数",
        "default": 100,
        "hint": "超出后丢弃最旧的暂存消息",
        "slider": {
          "min": 10,
          "max": 1000,
          "step": 10
        }
      },
      "时间点容差秒": {
        "type": "int",
        "description": "「就是现在」判定容差（秒）",
        "default": 120,
        "hint": "LLM 填的 HH:MM 只刚过去该秒数内时视为就是现在，否则滚到明天同一时刻",
        "slider": {
          "min": 30,
          "max": 600,
          "step": 30
        }
      },
      "睡前提醒分钟": {
        "type": "int",
        "description": "睡前提前多少分钟提醒一次 LLM",
        "default": 5,
        "hint": "到点向会话发一次「睡前提醒内容」，LLM 可主动说要去睡觉或陪用户聊几句；0 表示关闭",
        "slider": {
          "min": 0,
          "max": 180,
          "step": 5
        }
      },
      "睡前提醒随机误差秒": {
        "type": "int",
        "description": "睡前提醒的随机误差（秒）",
        "default": 120,
        "hint": "提醒时刻在此基础上随机偏移 ±该秒数，避免每天准点；0 表示准点",
        "slider": {
          "min": 0,
          "max": 600,
          "step": 1
        }
      },
      "睡前提醒内容": {
        "type": "text",
        "description": "睡前提醒投递给 LLM 的提示词",
        "hint": "自动包裹 <system_reminder>；{n} 为距入睡的分钟数；留空关闭",
        "default": "**(Sleep task)** You have **{n}** minutes left before bedtime. You can use this time to let the user know you're about to head to sleep, or stay and chat for a little while longer. When the time's up, the sleep task will automatically put you to sleep."
      },
      "醒来提示": {
        "type": "text",
        "description": "醒来汇总消息时附加给 LLM 的提示语",
        "hint": "插件会自动包裹 <system_reminder>，支持 {数量} 占位符",
        "default": "You have just woken up. The {数量} messages above were sent to you while you were asleep. Each entry starts with [Message N; yyyy-mm-dd HH:MM:SS], where N is the arrival order and the timestamp shows when it was received. Multiple messages sent in rapid succession may have been merged into a single entry, shown as [Message N; yyyy-mm-dd HH:MM:SS] [Message M] content, where the inner [Message M] marks the original order of each merged message. Respond to them naturally: briefly acknowledge that you were resting, then reply to the messages in order."
      }
    }
  }
}
